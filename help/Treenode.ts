class TreeNode {
    val: number
    left: TreeNode
    right: TreeNode
    constructor(var?: number, left?: TreeNode | null, right?: TreeNode | null) {
        this.val = (val === undefined ? 0 : val)
        this.left = (left === undefined | null ? null : left)
        this.right = (right === undefined | null ? null : rigiht)
    }
}